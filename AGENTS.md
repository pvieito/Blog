# Agents

Read `README.md` for project-specific guidance when present.

For shared preview and deployment preparation, use the `blog-guide` skill.
For local previews, use the `blog-preview` skill.
For production deployments, use the `blog-deploy` skill.

Keep project-specific guidance in `README.md` or source-local documentation, not in this shared `AGENTS.md`.

## Public Repository

**IMPORTANT**: This repository is publicly visible website source, not a public library or API. Internal tooling may be named and invoked when relevant to maintaining the site. Keep proprietary implementations, credentials, private inputs, and confidential data out of published source and generated output.

Internal generators and scripts live under the ignored `extra/Scripts/` directory and must remain excluded from Git and site publication. Preserve the site's public content, intended behavior, and stable URLs.
